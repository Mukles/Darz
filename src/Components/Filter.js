import React from 'react';
import Sidebar from './Sidebar';

const Filter = () =>{
  return(
    <section id='Fitler-data' className='col-md-3 border-right'>
      <Sidebar />
    </section>
  )
}

export default Filter;
